server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kapildevsharma/spring-microservices/config-server.git
          clone-on-start: true